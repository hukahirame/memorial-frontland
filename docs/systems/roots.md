---
id: roots
slice: 根源 🌳
touches: []
tunables: []
---

## 体験の意図

（未記入。人間が書く。Notion から転記するか、ここで決める）

## デフォルトと違う点

- 攻略度に上限が無い。100 を超えて伸びる
- 危険度はどこからも書き換えられない。生成時の値のまま表示される
- 氾濫の判定は、蓄積値を足した直後、攻略度を減らす前に行う。順序に意味がある
- 根源は同じ Id を二重に持たない。MainSite へ戻るたび `Start` が再実行されるため

## 相互作用

- WHEN 零時、蓄積値が 10 増え、攻略度が 3 減る
- WHEN 蓄積値が 100 以上、氾濫。敵が 5 体湧く
- WHEN 敵を 1 体倒した、蓄積値が 3 減る（[enemy](enemy.md) が下げる）
- WHEN クエストを達成した、報酬の `progress` のぶん攻略度が上がる（[quest](quest.md)）
- WHEN 根源のシーンへ初めて入った、候補から 1 つ選ばれてスポーン地点が決まる。
  2 回目以降は動かない
- WHILE 攻略度が 50 以上、敵の湧きが抑えられる（[enemy](enemy.md)）
- WHILE 攻略度が 30 以上、湧いた敵の体力が減る（[enemy](enemy.md)）

## やらないこと

- 日数で根源が増えること。`RootCreate` は `Start` でしか呼ばれず、2 つで固定
- 根源変化。`RootTrick` は毎日 50% で呼ばれるが中身が空

## 未決

- 攻略度の上限。100 を超えたときに何が起きるべきか
- 危険度 1000 / 2000 の使い道。いま表示に出るだけで、計算に入っていない
- 氾濫したあと蓄積値は 100 のまま留まる。下げる手段が敵の討伐しかない
- 根源が保存されない。`SaveData` に入っていないので、再起動で攻略度も蓄積値も消える

## 確認

- 状態: はじまりの根源、蓄積値 90、零時の直前
- 見るもの: （未記入。人間が書く）

## 調整値

いずれも ScriptableObject に出ていない。`tune` で触れない。

| 値 | 居場所 |
|---|---|
| 日次の蓄積値 +10 | `Root.DailyAccumulationGain` |
| 日次の攻略度 -3 | `Root.DailyProgressLoss` |
| 段階の閾値 15 / 40 / 75 / 100 | `Root.Level` |
| 氾濫時の湧き数 5 | `RootsManager.StampedeJudge` |
| 根源2つの危険度 1000 / 2000 | `RootsManager.Start` |
